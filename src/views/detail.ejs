<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notas</title>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

    <header>
        <nav>
            <div class="brand-wrapper">
                <div class="logo">
                    <img src="/images/logo.svg" alt="">
                </div>
            </div>        
        </nav>
    </header>

    <main>
        <section class="editForm">
            <h1>Editar una nota</h1>
            <form method="POST" action="/edit/<%= nota[0].id %>?_method=PUT">
                <div>
                    <label for="">Título</label>
                    <input type="text" name="title" value="<%= typeof nota != 'undefined' && nota.length > 0 ? nota[0].titulo : '' %>">
                </div>
                <div>
                    <label for="message">Mensaje</label>
                    <textarea name="message" id="message" cols="30" rows="10"><%= typeof nota != 'undefined' ? nota[0].texto : '' %></textarea>
                </div>
                <button type="submit">Editar</button>
                <button type="button" onclick="javascript: window.location = '/';">Volver</button>
            </form>
        </section>
    </main>

</body>
</html>